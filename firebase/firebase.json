{
  "emulators": {
    "auth": {
      "port": "${FIREBASE_AUTHENTICATION_PORT}",
      "host": "0.0.0.0"
    },
    "ui": {
      "enabled": true,
      "port": "${FIREBASE_UI_PORT}",
      "host": "0.0.0.0"
    },
    "singleProjectMode": true
  }
}
